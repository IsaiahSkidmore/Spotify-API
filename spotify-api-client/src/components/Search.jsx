import classes from '../styles/Search.module.css';

function Search() {
    return (
        <div className={classes.search}>
        <h1 className={`${classes.searchH1}`}>Search</h1>
        </div>
    );
}

export default Search;
